# 🚀 Инструкция по развертыванию системы

## Предварительные требования

- Node.js и npm установлены
- Аккаунт Google для Firebase
- Аккаунт EmailJS (для email уведомлений)

## Пошаговая инструкция

### Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Добавить проект" (Add project)
3. Введите название проекта: `dental-clinic` (или своё название)
4. Следуйте инструкциям мастера создания проекта

### Шаг 2: Настройка Firebase Authentication

1. В Firebase Console откройте раздел "Authentication"
2. Перейдите на вкладку "Sign-in method"
3. Включите следующие методы:
   - Email/Password
   - Google

### Шаг 3: Создание базы данных Firestore

1. В Firebase Console откройте раздел "Firestore Database"
2. Нажмите "Создать базу данных"
3. Выберите режим "Production mode"
4. Выберите локацию (например, europe-west1)

### Шаг 4: Получение конфигурации Firebase

1. В Firebase Console перейдите в настройки проекта (⚙️ Settings)
2. Прокрутите вниз до раздела "Your apps"
3. Нажмите на иконку веб-приложения (</>)
4. Зарегистрируйте приложение с именем "Dental Clinic Web"
5. Скопируйте объект конфигурации `firebaseConfig`

### Шаг 5: Обновление конфигурации в коде

Откройте файл `js/firebase-config.js` и замените конфигурацию:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSy...",
    authDomain: "dental-clinic-xxxxx.firebaseapp.com",
    projectId: "dental-clinic-xxxxx",
    storageBucket: "dental-clinic-xxxxx.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abcdef"
};
```

### Шаг 6: Установка Firebase CLI

```bash
npm install -g firebase-tools
```

### Шаг 7: Вход в Firebase CLI

```bash
firebase login
```

### Шаг 8: Инициализация Firebase в проекте

```bash
cd /path/to/dental-clinic
firebase init
```

Выберите следующие опции:
- Hosting: Configure files for Firebase Hosting
- Firestore: Deploy rules and create indexes

Настройки:
- Public directory: `.` (текущая директория)
- Configure as single-page app: Yes
- Set up automatic builds: No
- Overwrite index.html: No

### Шаг 9: Обновление .firebaserc

Откройте `.firebaserc` и замените на ID вашего проекта:

```json
{
  "projects": {
    "default": "dental-clinic-xxxxx"
  }
}
```

### Шаг 10: Развертывание правил Firestore

```bash
firebase deploy --only firestore:rules
firebase deploy --only firestore:indexes
```

### Шаг 11: Развертывание на Firebase Hosting

```bash
firebase deploy --only hosting
```

После успешного развертывания вы получите URL вашего приложения, например:
`https://dental-clinic-xxxxx.web.app`

## Настройка Email уведомлений (EmailJS)

### Шаг 1: Регистрация на EmailJS

1. Перейдите на [EmailJS](https://www.emailjs.com/)
2. Зарегистрируйтесь (Sign Up)

### Шаг 2: Добавление Email Service

1. В панели EmailJS перейдите в "Email Services"
2. Нажмите "Add New Service"
3. Выберите Gmail (или другой провайдер)
4. Подключите свой email аккаунт
5. Скопируйте Service ID

### Шаг 3: Создание Email Templates

Создайте следующие шаблоны:

#### Шаблон подтверждения записи

```
Subject: Подтверждение записи в ДентаКлиник

Здравствуйте, {{patient_name}}!

Ваша запись подтверждена:
- Врач: {{doctor_name}}
- Услуга: {{service_name}}
- Дата: {{date}}
- Время: {{time}}

С уважением,
ДентаКлиник
```

#### Шаблон отмены записи

```
Subject: Отмена записи в ДентаКлиник

Здравствуйте, {{patient_name}}!

Ваша запись на {{date}} в {{time}} была отменена.
Причина: {{reason}}

С уважением,
ДентаКлиник
```

#### Шаблон рекомендаций

```
Subject: Новые рекомендации от врача

Здравствуйте, {{patient_name}}!

Врач {{doctor_name}} оставил для вас рекомендации:
{{recommendations}}

С уважением,
ДентаКлиник
```

#### Шаблон чека

```
Subject: Электронный чек №{{receipt_number}}

Здравствуйте, {{patient_name}}!

Во вложении ваш электронный чек №{{receipt_number}}.
Дата: {{date}}
Сумма: {{total_amount}} ₽

С уважением,
ДентаКлиник
```

### Шаг 4: Обновление конфигурации EmailJS

Откройте `js/notifications.js` и обновите:

```javascript
const EMAILJS_PUBLIC_KEY = 'your_public_key';
const EMAILJS_SERVICE_ID = 'your_service_id';
const EMAILJS_TEMPLATE_ID = 'your_template_id';
```

### Шаг 5: Подключение EmailJS SDK

Добавьте в `index.html` перед закрывающим тегом `</body>`:

```html
<script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
```

## Добавление библиотеки для PDF

Для генерации PDF чеков добавьте в `index.html`:

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
```

## Создание первого администратора

После развертывания:

1. Зарегистрируйте пользователя через форму регистрации
2. Перейдите в Firebase Console → Firestore Database
3. Найдите созданного пользователя в коллекции `users`
4. Измените поле `role` на `admin`

## Добавление тестовых данных

### Создание услуг

В Firestore Console создайте коллекцию `services` с документами:

```javascript
// Документ 1
{
  name: "Консультация стоматолога",
  category: "Консультация",
  price: 1500,
  description: "Первичный осмотр и консультация"
}

// Документ 2
{
  name: "Лечение кариеса",
  category: "Терапия",
  price: 3500,
  description: "Лечение кариеса с установкой пломбы"
}

// Документ 3
{
  name: "Профессиональная чистка",
  category: "Гигиена",
  price: 2500,
  description: "Ультразвуковая чистка зубов"
}

// Документ 4
{
  name: "Отбеливание зубов",
  category: "Эстетика",
  price: 15000,
  description: "Профессиональное отбеливание"
}
```

### Создание врача

1. Зарегистрируйте пользователя
2. В Firestore измените `role` на `doctor`
3. Добавьте поле `specialty`: "Стоматолог-терапевт"

## Проверка работы

1. Откройте приложение по URL
2. Зарегистрируйтесь как пациент
3. Просмотрите список услуг
4. Создайте запись на прием
5. Войдите как врач (другой браузер/инкогнито)
6. Проверьте расписание
7. Войдите как администратор
8. Проверьте все функции администратора

## Обновление приложения

После внесения изменений в код:

```bash
# Развернуть все изменения
firebase deploy

# Или только хостинг
firebase deploy --only hosting

# Или только правила
firebase deploy --only firestore:rules
```

## Локальная разработка

Для локальной разработки:

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## Отладка

### Просмотр логов

```bash
firebase functions:log
```

### Эмуляторы Firebase

Для локальной разработки с эмуляторами:

```bash
firebase emulators:start
```

## Безопасность

⚠️ **Важно:**

1. Никогда не коммитьте API ключи в публичный репозиторий
2. Проверьте правила безопасности Firestore
3. Настройте домен для аутентификации в Firebase Console
4. Включите App Check для защиты от ботов

## Мониторинг

В Firebase Console настройте:

1. Performance Monitoring
2. Crashlytics (для мобильных приложений)
3. Analytics

## Резервное копирование

Настройте автоматическое резервное копирование Firestore:

```bash
gcloud firestore export gs://[BUCKET_NAME]
```

## Поддержка

При возникновении проблем:

1. Проверьте консоль браузера (F12)
2. Проверьте Firebase Console → Authentication/Firestore
3. Проверьте правила безопасности
4. Проверьте индексы Firestore

## Полезные ссылки

- [Firebase Documentation](https://firebase.google.com/docs)
- [Firestore Security Rules](https://firebase.google.com/docs/firestore/security/get-started)
- [EmailJS Documentation](https://www.emailjs.com/docs/)
- [html2pdf.js Documentation](https://github.com/eKoopmans/html2pdf.js)
