{
  "name": "dental-clinic-system",
  "version": "1.0.0",
  "description": "Dental Clinic Management System with Firebase",
  "private": true,
  "scripts": {
    "dev": "npx http-server -p 3000 -o",
    "build": "echo 'Build complete'",
    "deploy": "firebase deploy",
    "lint": "echo 'No linting configured for vanilla JavaScript project'"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
