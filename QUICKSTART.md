# 🚀 Быстрый старт

## Минимальная настройка для запуска проекта

### Шаг 1: Установка зависимостей

```bash
npm install
```

### Шаг 2: Создание проекта Firebase

1. Перейдите на https://console.firebase.google.com/
2. Создайте новый проект
3. Включите:
   - **Authentication** → Email/Password и Google
   - **Firestore Database** → Production mode

### Шаг 3: Получение конфигурации

1. В Firebase Console → Project Settings → Your apps
2. Нажмите на иконку веб-приложения (</>)
3. Скопируйте конфигурацию

### Шаг 4: Обновление конфигурации

Откройте `js/firebase-config.js` и вставьте свою конфигурацию:

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

### Шаг 5: Локальный запуск

```bash
npm run dev
```

Откройте http://localhost:3000

### Шаг 6: Создание первого администратора

1. Зарегистрируйтесь через форму регистрации
2. Перейдите в Firebase Console → Firestore Database
3. Найдите созданного пользователя в коллекции `users`
4. Измените поле `role` с `patient` на `admin`
5. Обновите страницу в браузере

### Шаг 7: Добавление тестовых услуг

В Firebase Console → Firestore → Создайте коллекцию `services`:

```javascript
// Документ 1
{
  name: "Консультация стоматолога",
  category: "Консультация",
  price: 1500,
  description: "Первичный осмотр"
}

// Документ 2
{
  name: "Лечение кариеса",
  category: "Терапия",
  price: 3500,
  description: "Лечение кариеса с пломбой"
}
```

### Шаг 8: Развертывание правил безопасности

```bash
# Установите Firebase CLI
npm install -g firebase-tools

# Войдите в Firebase
firebase login

# Обновите .firebaserc с ID вашего проекта
# Затем разверните правила
firebase deploy --only firestore:rules
firebase deploy --only firestore:indexes
```

### Шаг 9: Развертывание на Firebase Hosting (опционально)

```bash
firebase deploy --only hosting
```

## Тестирование системы

### Тест 1: Пациент
1. Зарегистрируйтесь как новый пользователь
2. Просмотрите услуги
3. Создайте запись на прием

### Тест 2: Врач
1. В Firestore создайте пользователя с `role: "doctor"`
2. Войдите с этими данными
3. Просмотрите расписание

### Тест 3: Администратор
1. Войдите как администратор
2. Добавьте/измените услугу
3. Просмотрите статистику

## Проблемы и решения

### Ошибка: "Missing or insufficient permissions"
- Проверьте, что развернуты правила Firestore: `firebase deploy --only firestore:rules`

### Ошибка: "Firebase: Error (auth/...)"
- Проверьте, что включен Email/Password в Authentication
- Проверьте правильность конфигурации в firebase-config.js

### Не работает навигация
- Очистите кеш браузера
- Проверьте консоль браузера (F12) на ошибки

### Не отображаются данные
- Проверьте, что созданы индексы: `firebase deploy --only firestore:indexes`
- Проверьте консоль Firestore на наличие данных

## Следующие шаги

После базовой настройки:

1. 📧 Настройте EmailJS для уведомлений (см. DEPLOYMENT.md)
2. 📄 Подключите html2pdf для генерации чеков
3. 🎨 Кастомизируйте дизайн в css/styles.css
4. 🔐 Настройте дополнительные правила безопасности

## Полезные ссылки

- [Полная документация](README.md)
- [Инструкция по развертыванию](DEPLOYMENT.md)
- [Примеры API](API_EXAMPLES.md)
- [Структура проекта](STRUCTURE.md)

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте Firebase Console
3. Проверьте документацию в файлах выше
