# 📁 Структура проекта

## Основные файлы

```
dental-clinic/
├── index.html                 # Главная страница приложения (точка входа)
├── package.json               # Зависимости и скрипты npm
├── firebase.json              # Конфигурация Firebase Hosting
├── firestore.rules            # Правила безопасности Firestore
├── firestore.indexes.json     # Индексы Firestore
├── .firebaserc                # Firebase CLI конфигурация
├── .gitignore                 # Игнорируемые файлы Git
├── README.md                  # Основная документация
├── DEPLOYMENT.md              # Инструкция по развертыванию
├── API_EXAMPLES.md            # Примеры использования API
└── STRUCTURE.md               # Этот файл
```

## Директории

### /css - Стили
```
css/
├── styles.css        # Основные стили (кнопки, формы, карточки, навигация)
├── responsive.css    # Адаптивный дизайн (медиа-запросы)
└── theme.css         # Светлая/темная тема
```

### /js - JavaScript модули
```
js/
├── firebase-config.js   # Конфигурация Firebase
├── auth.js              # Аутентификация и управление пользователями
├── patient.js           # Функции для роли "Пациент"
├── doctor.js            # Функции для роли "Врач"
├── admin.js             # Функции для роли "Администратор"
├── notifications.js     # Email уведомления и PDF генерация
├── ui.js                # Управление UI и навигация
├── utils.js             # Вспомогательные функции
└── app.js               # Точка входа приложения
```

### /html - HTML страницы (заготовки)
```
html/
└── (можно добавить отдельные HTML шаблоны)
```

### /assets - Ресурсы
```
assets/
└── images/
    └── favicon.png      # Иконка сайта
```

### /public - Публичные файлы
```
public/
└── (статические файлы, если нужны)
```

## Описание модулей

### index.html
- Главная страница SPA (Single Page Application)
- Подключение Firebase SDK из CDN
- Подключение всех CSS и JS модулей
- Структура навигации и контента

### firebase-config.js
- Инициализация Firebase App
- Экспорт auth и db объектов
- Содержит конфигурацию Firebase (API ключи)

### auth.js
**Функции аутентификации:**
- `login()` - вход в систему
- `register()` - регистрация нового пациента
- `loginWithGoogle()` - вход через Google
- `logout()` - выход из системы
- `resetPassword()` - восстановление пароля
- `onAuthChange()` - слушатель изменений аутентификации
- `getCurrentUser()` - получить текущего пользователя
- `getCurrentUserData()` - получить данные пользователя
- `hasRole()` - проверка роли пользователя

### patient.js
**Функции для пациентов:**
- `getServices()` - получить список услуг
- `getDoctors()` - получить список врачей
- `createAppointment()` - создать запись на прием
- `getPatientAppointments()` - получить свои записи
- `getPatientHistory()` - получить историю визитов
- `createFeedback()` - оставить отзыв
- `getPatientFeedbacks()` - получить свои отзывы
- `updatePatientProfile()` - обновить профиль

### doctor.js
**Функции для врачей:**
- `getDoctorSchedule()` - получить расписание
- `getDoctorPatients()` - получить список пациентов
- `getPatientMedicalHistory()` - история пациента
- `updateAppointmentNotes()` - обновить заметки
- `completeAppointment()` - завершить прием
- `getDoctorStatistics()` - получить статистику
- `getDoctorFeedbacks()` - получить отзывы
- `searchPatient()` - поиск пациента

### admin.js
**Функции для администраторов:**

**Услуги:**
- `getAllServices()` - получить все услуги
- `addService()` - добавить услугу
- `updateService()` - обновить услугу
- `deleteService()` - удалить услугу

**Записи:**
- `getAllAppointments()` - получить все записи
- `cancelAppointment()` - отменить запись

**Врачи:**
- `getAllDoctors()` - получить всех врачей
- `updateDoctorSchedule()` - обновить расписание врача

**Материалы:**
- `getAllMaterials()` - получить все материалы
- `addMaterial()` - добавить материал
- `updateMaterial()` - обновить материал
- `deleteMaterial()` - удалить материал

**Статистика:**
- `getAllTransactions()` - получить транзакции
- `getAdminStatistics()` - общая статистика

### notifications.js
**Email и PDF:**
- `initEmailJS()` - инициализация EmailJS
- `sendAppointmentNotification()` - уведомление о записи
- `sendCancellationNotification()` - уведомление об отмене
- `sendRecommendationsNotification()` - уведомление о рекомендациях
- `sendReceipt()` - отправить чек
- `generateReceiptPDF()` - генерация PDF чека

### ui.js
**Управление интерфейсом:**
- `initUI()` - инициализация UI
- `updateNavigation()` - обновить навигацию по роли
- `navigate()` - навигация между страницами
- Функции загрузки страниц для каждой роли

### utils.js
**Вспомогательные функции:**
- `showLoading()` / `hideLoading()` - индикатор загрузки
- `showNotification()` - показать уведомление
- `formatDate()` / `formatTime()` / `formatDateTime()` - форматирование дат
- `formatPrice()` - форматирование цен
- `validateEmail()` / `validatePhone()` - валидация
- `createModal()` - создать модальное окно
- `confirm()` - диалог подтверждения
- `generateStarRating()` - генерация звезд рейтинга
- `toggleTheme()` - переключение темы
- `loadTheme()` - загрузка сохраненной темы

### app.js
**Главный файл:**
- Инициализация приложения
- Настройка обработчиков событий
- Обработка форм входа и регистрации
- Делегирование событий для динамического контента

## Структура данных Firestore

### Collections

#### users
```javascript
{
  email: string,
  fullName: string,
  phone: string,
  role: "patient" | "doctor" | "admin",
  createdAt: timestamp
}
```

#### patients
```javascript
{
  userId: string,
  medicalHistory: array,
  feedbacks: array,
  appointments: array
}
```

#### doctors
```javascript
{
  userId: string,
  specialty: string,
  schedule: object,
  rating: number,
  feedbacks: array
}
```

#### appointments
```javascript
{
  patientId: string,
  doctorId: string,
  serviceId: string,
  dateTime: timestamp,
  status: "scheduled" | "completed" | "cancelled",
  notes: string,
  diagnosis: string,
  createdAt: timestamp
}
```

#### services
```javascript
{
  name: string,
  category: string,
  price: number,
  description: string,
  createdAt: timestamp
}
```

#### materials
```javascript
{
  name: string,
  quantity: number,
  cost: number,
  date: timestamp
}
```

#### feedbacks
```javascript
{
  patientId: string,
  doctorId: string,
  appointmentId: string,
  rating: number (1-5),
  comment: string,
  date: timestamp
}
```

## Роли пользователей

### Patient (Пациент)
- Просмотр услуг
- Запись на прием
- Просмотр своих записей и истории
- Оставление отзывов
- Управление профилем

### Doctor (Врач)
- Просмотр расписания
- Просмотр пациентов
- Просмотр истории пациентов
- Редактирование заметок
- Завершение приемов
- Просмотр статистики и отзывов

### Admin (Администратор)
- Управление услугами (CRUD)
- Управление записями
- Управление расписанием врачей
- Учет материалов
- Генерация чеков
- Просмотр общей статистики

## Навигация (Роутинг)

Приложение использует SPA (Single Page Application) подход с клиентской навигацией.

### Общие страницы
- `home` - главная страница
- `services` - список услуг
- `login` - вход
- `register` - регистрация

### Страницы пациента
- `patient-dashboard` - мои записи
- `new-appointment` - новая запись
- `patient-history` - история визитов
- `patient-profile` - профиль

### Страницы врача
- `doctor-dashboard` - расписание
- `doctor-patients` - пациенты
- `doctor-statistics` - статистика
- `doctor-profile` - профиль

### Страницы администратора
- `admin-dashboard` - панель управления
- `admin-services` - управление услугами
- `admin-appointments` - управление записями
- `admin-doctors` - управление врачами
- `admin-materials` - учет материалов
- `admin-receipts` - электронные чеки

## Стили и темизация

### CSS переменные (светлая тема)
```css
--primary-color: #2c3e50
--secondary-color: #3498db
--accent-color: #e74c3c
--success-color: #27ae60
--warning-color: #f39c12
--danger-color: #e74c3c
--bg-color: #ffffff
--surface-color: #f8f9fa
--text-color: #2c3e50
```

### Темная тема
Автоматически переключается через атрибут `data-theme="dark"` на `<html>`.

### Адаптивность
- Desktop: > 992px
- Tablet: 768px - 992px
- Mobile: < 768px
- Small Mobile: < 480px

## Безопасность

### Firestore Rules
- Пациенты могут читать/писать только свои данные
- Врачи могут читать данные пациентов и писать в записи
- Администраторы имеют полный доступ
- Услуги доступны всем для чтения

### Authentication
- Email/Password с валидацией
- Google OAuth
- Восстановление пароля
- Проверка ролей на клиенте и в Firestore Rules

## Зависимости

### Runtime
- Firebase SDK 10.7.1 (из CDN)
  - firebase-app
  - firebase-auth
  - firebase-firestore

### Development
- http-server (для локальной разработки)

### Опциональные (подключаются через CDN)
- EmailJS (для email уведомлений)
- html2pdf.js (для генерации PDF)

## Команды NPM

```bash
npm run dev      # Запустить локальный сервер
npm run build    # Подготовка к продакшену
npm run deploy   # Развертывание на Firebase Hosting
```

## Дополнительные файлы

- `.gitignore` - файлы для игнорирования в Git
- `.firebaserc` - конфигурация Firebase CLI
- `package-lock.json` - зависимости (автогенерируется)

## Рекомендации по расширению

### Добавление новой страницы
1. Создайте функцию `loadXxxPage()` в `ui.js`
2. Добавьте роут в `navigate()` функцию
3. Добавьте ссылку в навигацию в `updateNavigation()`

### Добавление новой функции
1. Создайте функцию в соответствующем модуле (patient.js, doctor.js, admin.js)
2. Импортируйте и используйте в UI
3. Добавьте пример использования в API_EXAMPLES.md

### Добавление нового поля в коллекцию
1. Обновите структуру в коде
2. Обновите Firestore Rules при необходимости
3. Создайте миграцию данных если нужно

## Производительность

- Используются Firebase индексы для быстрых запросов
- Lazy loading для больших списков
- Кеширование данных пользователя
- Оптимизированные CSS и JS

## Поддержка браузеров

- Chrome (последняя версия)
- Firefox (последняя версия)
- Safari (последняя версия)
- Edge (последняя версия)
- Мобильные браузеры (iOS Safari, Chrome Mobile)
